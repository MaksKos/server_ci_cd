name: Server tests

on: push

jobs:
    test:
        name: Testing server
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: test
              run: python3 -m unittest test.py